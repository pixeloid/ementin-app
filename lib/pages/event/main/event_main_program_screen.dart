import 'package:flutter/material.dart';

class EventMainProgramScreen extends StatelessWidget {
  const EventMainProgramScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_car);
  }
}
