import 'package:flutter/material.dart';

class PEventProgram extends StatelessWidget {
  const PEventProgram({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Event PROGRAM'),
      ),
    );
  }
}
